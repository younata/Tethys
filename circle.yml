machine:
  xcode:
    version: 7.3

dependencies:
  override:
    - brew update
    - brew upgrade carthage || true
    - bundle
    - bundle exec fastlane setup

test:
  override:
    - bundle exec fastlane test

