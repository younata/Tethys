---
platform: darwin

inputs:
- name: code

caches:
- path: code/Carthage

run:
  path: code/concourse/deploy.sh

params:
  FASTLANE_PASSWORD:
  FASTLANE_USER:
  PILOT_USERNAME:
  PASIPHAE_TOKEN:
  PASIPHAE_URL:
  PILOT_APP_IDENTIFIER:

