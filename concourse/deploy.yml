---
platform: darwin

inputs:
- name: rssclient

run:
  path: rssclient/concourse/deploy.sh

params:
  FASTLANE_PASSWORD: {{FASTLANE_PASSWORD}}
  FASTLANE_USER: {{FASTLANE_USER}}
  PILOT_USERNAME: {{PILOT_USERNAME}}
  MIXPANEL_TOKEN: {{MIXPANEL_TOKEN}}
  PASIPHAE_TOKEN: {{PASIPHAE_TOKEN}}
  PASIPHAE_URL: {{PASIPHAE_URL}}

