---
platform: darwin

inputs:
- name: rssclient

run:
  path: rssclient/concourse/deploy.sh

params:
  FASTLANE_PASSWORD:
  FASTLANE_USER:
  PILOT_USERNAME:
  MIXPANEL_TOKEN:
  PASIPHAE_TOKEN:
  PASIPHAE_URL:

